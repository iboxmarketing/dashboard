# Server-side only. Configure production through ChatGPT Sites runtime secrets.
# For local development, copy this file to ignored `.env.local` and fill it there.
# Never commit or paste the real Bitrix REST webhook into an AI prompt.
BITRIX24_WEBHOOK_URL=
